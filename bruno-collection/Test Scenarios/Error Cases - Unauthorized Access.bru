meta {
  name: Error Cases - Unauthorized Access
  type: http
  seq: 3
}

post {
  url: {{apiUrl}}/tokens/generate
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Should require authentication", function() {
    expect(res.getStatus()).to.equal(401);
  });
  
  test("Should return authentication error", function() {
    expect(res.getBody().error).to.include("Authentication");
  });
}