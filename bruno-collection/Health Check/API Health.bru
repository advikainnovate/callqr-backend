meta {
  name: API Health
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/health
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Should return API health info", function() {
    expect(res.getBody().service).to.equal("privacy-qr-calling-backend");
  });
}