meta {
  name: Get Routing Stats
  type: http
  seq: 6
}

get {
  url: {{apiUrl}}/calls/admin/stats
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

headers {
  Content-Type: application/json
}

tests {
  test("Get stats should succeed", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Should return statistics", function() {
    expect(res.getBody().timestamp).to.exist;
  });
}