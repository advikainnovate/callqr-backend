meta {
  name: Terminate Call
  type: http
  seq: 4
}

delete {
  url: {{apiUrl}}/calls/{{sessionId}}
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Call termination should succeed", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Should confirm termination", function() {
    expect(res.getBody().message).to.include("terminated");
  });
}