meta {
  name: Mark Message as Read
  type: http
  seq: 9
}

patch {
  url: {{baseUrl}}/api/messages/:messageId/read
  body: none
  auth: bearer
}

params:path {
  messageId: {{messageId}}
}

auth:bearer {
  token: {{authToken}}
}
