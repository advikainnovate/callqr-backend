meta {
  name: Get Chat Details
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/chat-sessions/:chatSessionId
  body: none
  auth: bearer
}

params:path {
  chatSessionId: your-chat-session-id
}

auth:bearer {
  token: {{authToken}}
}
