meta {
  name: Mark Chat as Read
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/api/messages/chat/:chatSessionId/read
  body: none
  auth: bearer
}

params:path {
  chatSessionId: {{chatSessionId}}
}

auth:bearer {
  token: {{authToken}}
}
