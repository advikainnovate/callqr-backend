meta {
  name: "Get Active Calls"
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/calls/active
  body: none
  auth: none
}

tests {
  test("Should get active calls", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.be.true;
    expect(res.body.data.activeCalls).to.be.an('array');
  });
}
