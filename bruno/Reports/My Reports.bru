meta {
  name: "My Reports"
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/reports/my-reports
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("Should get my reports successfully", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.be.true;
    expect(res.body.data.reports).to.be.an('array');
  });
}
