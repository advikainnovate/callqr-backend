meta {
  name: Admin
  type: folder
}

docs {
  Admin dashboard endpoints for managing users, QR codes, calls, and chats.
  
  Note: All endpoints require authentication. In production, add admin role middleware.
}
