meta {
  name: Delete User
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/admin/users/:userId
  body: none
  auth: bearer
}

params:path {
  userId: user-id-here
}

auth:bearer {
  token: {{authToken}}
}

docs {
  Soft delete user (sets status to 'deleted')
}
