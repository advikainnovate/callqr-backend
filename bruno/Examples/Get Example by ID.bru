meta {
  name: "Get Example by ID"
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/examples/{{exampleId}}
  body: none
  auth: none
}

tests {
  test("Should get example by ID", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.be.true;
    expect(res.body.data.example.id).to.be.a('string');
  });
}
