meta {
  name: "Search Examples"
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/examples/search?q=test
  body: none
  auth: none
}

tests {
  test("Should search examples", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.be.true;
    expect(res.body.data.examples).to.be.an('array');
  });
}
