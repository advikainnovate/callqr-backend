meta {
  name: "Delete Example"
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/examples/{{exampleId}}
  body: none
  auth: none
}

tests {
  test("Should delete example successfully", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.be.true;
    expect(res.body.message).to.equal('Example deleted successfully');
  });
}
