meta {
  name: "Get Examples by Category"
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/examples/category/test
  body: none
  auth: none
}

tests {
  test("Should get examples by category", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.be.true;
    expect(res.body.data.examples).to.be.an('array');
  });
}
